{
  "status": "completed",
  "merged_branches": [
    "agent/worker-1"
  ],
  "merge_commits": [
    "2cbeff83890f02d09c36dc082ca2739b594f090d"
  ],
  "tests": [
    {
      "command": "cargo fmt --all -- --check",
      "result": "pass"
    },
    {
      "command": "cargo test --locked --all-targets",
      "result": "pass"
    },
    {
      "command": "cargo clippy --locked --all-targets -- -D warnings",
      "result": "pass"
    },
    {
      "command": "cargo test --locked --no-default-features --features egui-ui --lib --bin lastkey-settings --test ui_semantic",
      "result": "pass"
    },
    {
      "command": "cargo clippy --locked --no-default-features --features egui-ui --all-targets -- -D warnings",
      "result": "pass"
    },
    {
      "command": "cargo check --locked --target x86_64-unknown-linux-gnu --all-targets",
      "result": "pass"
    },
    {
      "command": "cargo tree --no-default-features -e normal",
      "result": "pass"
    },
    {
      "command": "git diff --check",
      "result": "pass"
    },
    {
      "command": ".github\\scripts\\Test-ReleaseFormDefault.ps1",
      "result": "pass"
    }
  ],
  "conflicts_resolved": [],
  "unresolved_conflicts": [],
  "risks": [
    "agent/worker-1 T11 round-2 rework (a0d71d9 residual Iced/ui2 reference cleanup in Cargo.toml comment, docs/architecture/engine.md flow labels, AGENTS.md, and report + 2cbeff8 T11 report) was a direct descendant of agent/integration at f5e4535, so git fast-forwarded to 2cbeff8 with no merge commit and no conflicts; changed files are Cargo.toml (comment path only), docs/architecture/engine.md (comment labels only), and .orchestrator/reports/T11.json.",
    "Docs/comment-only production change: no code, feature, or dependency input changed, so gate counts are unchanged (default 72+9+15+18+18, egui-ui 199 lib + 0 bin + 6 semantic). Full updated gate re-run on the final tree passes all 9 commands; default tree remains GUI-free.",
    "Native visual acceptance remains uncovered per docs/architecture/verification.md Unverified Areas; test success is not visual evidence."
  ]
}
